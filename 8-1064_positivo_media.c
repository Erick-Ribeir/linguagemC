#include <stdio.h>
int main(){
	int cont, num, contPositivo=0;
	double soma=0,media;
	for (cont=1;cont<=6;cont++){
		scanf("%d",&num);
		if(num>0){
			contPositivo++
			soma=soma+num
		}//fim if
	}//fim for
}
